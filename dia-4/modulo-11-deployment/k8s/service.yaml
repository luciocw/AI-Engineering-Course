# Placeholder - sera implementado no Modulo 11
apiVersion: v1
kind: Service
metadata:
  name: ai-service
spec:
  selector:
    app: ai-service
  ports:
    - port: 80
      targetPort: 3000
  type: LoadBalancer
